# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Redis / Celery
redis==5.1.1
celery==5.4.0

# HTTP Client
httpx==0.27.2

# File handling
python-multipart==0.0.12
Pillow==10.4.0

# KYC Verification (OCR + Face matching)
pytesseract==0.3.10
# face-recognition installed separately in Dockerfile (dlib-bin)

# Utilities
python-slugify==8.0.4
python-dotenv==1.0.1

# Firebase (Push Notifications)
firebase-admin==6.5.0

# Email (optional — only if using SendGrid provider)
sendgrid==6.11.0

# Monitoring
sentry-sdk[fastapi]==2.14.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
